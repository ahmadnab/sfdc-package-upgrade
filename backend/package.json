{
  "name": "salesforce-upgrade-backend",
  "version": "1.0.0",
  "description": "Backend for Salesforce package upgrade automation",
  "main": "server.js",
  "scripts": {
    "start": "npx playwright install && node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.40.0",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}